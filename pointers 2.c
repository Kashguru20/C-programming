#include<stdio.h>
main()
{
	int *iptr;
	char *cptr;
	float *fptr;
	
	printf("the size of int pointer is %d",sizeof(iptr));
	printf("the size of the char pointer is %c",sizeof(cptr));
	printf(" the size of the float pointer is %f",sizeof(fptr));
	
	
}
